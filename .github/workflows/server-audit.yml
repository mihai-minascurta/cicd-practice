name: Nightly Server Audit

on:
  push:
    branches:
      - main
    paths:
      - 'audit.sh'

jobs:
  run-my-script:
    runs-on: ubuntu-latest
    steps:
      - name: Download the code
        uses: actions/checkout@v4

      - name: Execute the bash script
        run: ./audit.sh
